import react from "react"
import {Button, Card} from "react-bootstrap"

const AccountAccess = (option) => {

    <div className="mt-5" style={{height: '100vh', width: '100vw'}}>
        <div style={{ float: "left", width: "50%", height: '100%' }}></div>
        <div style={{ float: "right", width: "50%", height: '100%' }}></div>
    </div>

    if (option.toUpperCase() === "UP"){
        
    }else if (option.toUpperCase() === "IN"){

    }
}

const SignInOption = () =>{

}

const SignUpOption = () =>{

}

export default AccountAccess;